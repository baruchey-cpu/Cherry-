
<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Para Cherry 💘</title>

<style>

body{
    margin:0;
    font-family: 'Segoe UI', sans-serif;
    background: linear-gradient(135deg,#ff758c,#ff7eb3);
    color:white;
    display:flex;
    justify-content:center;
    align-items:center;
    height:100vh;
    overflow:hidden;
}

.card{
    background: rgba(0,0,0,0.35);
    padding:40px;
    border-radius:25px;
    width:90%;
    max-width:420px;
    text-align:center;
    backdrop-filter: blur(12px);
    box-shadow:0 10px 30px rgba(0,0,0,0.3);
    position:relative;
    z-index:2;
}

h1{
    margin-top:0;
}

img{
    width:170px;
    border-radius:18px;
    margin:15px 0;
    box-shadow:0 5px 15px rgba(0,0,0,0.4);
}

button{
    border:none;
    padding:15px 25px;
    margin:12px;
    font-size:18px;
    border-radius:14px;
    cursor:pointer;
    transition:0.25s;
}

#yes{
    background:#4CAF50;
    color:white;
}

#yes:active{
    transform:scale(1.1);
}

#no{
    background:#ff4d6d;
    color:white;
    position:relative;
}

/* corazones cayendo */

.heart{
    position:fixed;
    top:-10px;
    color:#ffccd5;
    animation: fall linear forwards;
    z-index:1;
}

@keyframes fall{
    to{
        transform: translateY(110vh);
        opacity:0;
    }
}

/* pantalla cuando dice que sí */

#overlay{
    position:fixed;
    inset:0;
    background:linear-gradient(135deg,#ff758c,#ff7eb3);
    display:flex;
    justify-content:center;
    align-items:center;
    text-align:center;
    font-size:28px;
    padding:40px;
    opacity:0;
    pointer-events:none;
    transition:0.6s;
    z-index:3;
}

#overlay.show{
    opacity:1;
    pointer-events:auto;
}

</style>
</head>

<body>

<div class="card">

<h1>💌 Para Cherry…</h1>

<p>
No necesito un día especial para recordarte cuánto te amo,  
pero sí quiero aprovechar este para hacerte una pregunta importante.<br><br>

Desde que estás conmigo, mis días son más bonitos,  
mis planes tienen más sentido  
y mi vida tiene más amor.<br><br>

¿Quieres ser mi San Valentín este año… y todos los que vengan? 💘
</p>

<img src="https://ibb.co/sJvY4dCt.jpg">

<br>

<button id="yes" onclick="aceptar()">💚 Sí quiero</button>
<button id="no">💔 No quiero</button>

</div>

<div id="overlay">
💘 Sabía que dirías que sí, Cherry 💘<br><br>
No te vas a arrepentir…<br>
me voy a encargar de hacerte muy feliz.
</div>

<script>

/* botón no huye */
const noBtn = document.getElementById("no");

noBtn.addEventListener("mouseover", () => {
    const x = Math.random()*250 -125;
    const y = Math.random()*250 -125;
    noBtn.style.transform = `translate(${x}px,${y}px)`;
});

/* cuando dice que sí */
function aceptar(){
    document.getElementById("overlay").classList.add("show");
    lanzarCorazones(40);
}

/* corazones cayendo */
function lanzarCorazones(num){
    for(let i=0;i<num;i++){
        const heart = document.createElement("div");
        heart.className="heart";
        heart.innerHTML="❤";
        heart.style.left=Math.random()*100+"vw";
        heart.style.fontSize=(Math.random()*20+15)+"px";
        heart.style.animationDuration=(Math.random()*3+2)+"s";
        document.body.appendChild(heart);
        setTimeout(()=>heart.remove(),5000);
    }
}

</script>

</body>
</html>